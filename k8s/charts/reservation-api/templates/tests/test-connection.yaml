apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "reservation-api.fullname" . }}-test-connection"
  labels:
    {{- include "reservation-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "reservation-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
